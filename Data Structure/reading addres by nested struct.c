#include <stdio.h>
struct addres{
	struct break_bown{
		char state[20];
		char town[20];
		char street[20];
	}b;
}a;
int main(){
	printf("enter the address in (state town street): formate");
	scanf("%s %s %s",a.b.state,a.b.town,a.b.street);
	print()
}
